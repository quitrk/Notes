{{#modal-widget title='Add new category'
                close="cancel" 
                ok="saveCategory"}}
  {{input type="text" class="form-control" placeholder="Category name..." value=category}}
{{/modal-widget}}